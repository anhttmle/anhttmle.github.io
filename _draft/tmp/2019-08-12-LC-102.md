---
title: "LeetCode #102 – Binary Tree Level Order Traversal"
date: 2019-08-12
layout: post
categories: LeetCodePractice
series: "LeetCode Practice"
series_part: 32
comments: true
mathjax: true
dsa_ref: 20
---

*Bài này áp dụng **BFS / Level Order** từ [DSA Understanding – Part 20](/dsafundamental/2018/10/12/DSA-20.html).*

## 1. Đề bài đầy đủ

Cho `root` binary tree. Trả về danh sách các **level**, mỗi level là list giá trị từ **trái sang phải**.

## 2. Ví dụ minh hoạ

```
    3
   / \
  9  20
    /  \
   15   7
```
→ `[[3], [9, 20], [15, 7]]`

## 3. Nhận diện pattern

+ **Level-by-level** → BFS + Queue
+ Mỗi vòng: xử lý đúng `size` phần tử hiện có trong queue = một level

## 4. Hướng tiếp cận từng bước

1. Nếu không root → `[]`
2. `queue = deque([root])`, `result = []`
3. While queue: `level_size = len(queue)`, pop `level_size` lần, collect vals, enqueue children không null
4. Append level vào result

## 5. Code Python

```python
from collections import deque


def levelOrder(root: TreeNode | None) -> list[list[int]]:
    if not root:
        return []

    result: list[list[int]] = []
    queue: deque[TreeNode] = deque([root])

    while queue:
        level_size = len(queue)
        level: list[int] = []
        for _ in range(level_size):
            node = queue.popleft()
            level.append(node.val)
            if node.left:
                queue.append(node.left)
            if node.right:
                queue.append(node.right)
        result.append(level)

    return result
```

## 6. Trace (cây trên)

| Queue (vals) | Level output |
|--------------|--------------|
| [3] | [3] |
| [9,20] | [9,20] |
| [15,7] | [15,7] |

## 7. Phân tích độ phức tạp

+ **Time O(n)**
+ **Space O(w)** — w = max width (~ n/2)

## 8. Biến thể

+ **#107** Level Order Bottom (reverse result)
+ **#103** Zigzag Level Order
+ **#199** Binary Tree Right Side View (last mỗi level)
