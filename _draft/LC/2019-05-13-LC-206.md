---
title: "LeetCode #206 – Reverse Linked List"
date: 2019-05-13
layout: post
categories: LeetCodePractice
series: "LeetCode Practice"
series_part: 19
comments: true
mathjax: true
dsa_ref: 13
---

*Bài này áp dụng **Linked List** từ [DSA Understanding – Part 13](/dsafundamental/2018/08/24/DSA-13.html).*

## 1. Đề bài đầy đủ

Cho `head` của singly linked list. **Đảo ngược** list và trả về `head` mới.

## 2. Ví dụ minh hoạ

| Input | Output |
|-------|--------|
| [1,2,3,4,5] | [5,4,3,2,1] |
| [1,2] | [2,1] |
| [] | [] |

## 3. Nhận diện pattern

+ Thao tác trên **con trỏ next** — không index ngẫu nhiên
+ **Iterative** O(1) extra space hoặc **recursive** O(n) stack
+ Giữ thứ tự đảo: từng cạnh đổi hướng một lần

## 4. Hướng tiếp cận từng bước

**Iterative:**

1. `prev = None`, `cur = head`
2. Khi `cur`: lưu `nxt = cur.next`, gán `cur.next = prev`, dịch `prev = cur`, `cur = nxt`
3. Trả `prev` (head mới)

**Tại sao lưu nxt trước?** Sau khi đổi `cur.next`, mất link tới phần còn lại.

## 5. Code Python

```python
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next


def reverseList(head: ListNode | None) -> ListNode | None:
    prev = None
    cur = head
    while cur:
        nxt = cur.next
        cur.next = prev
        prev = cur
        cur = nxt
    return prev
```

## 6. Trace through [1,2,3]

| Bước | prev | cur | Hành động |
|------|------|-----|-----------|
| 0 | None | 1 | nxt=2; 1→None |
| 1 | 1 | 2 | nxt=3; 2→1 |
| 2 | 2 | 3 | nxt=None; 3→2 |
| 3 | 3 | None | return 3 |

## 7. Phân tích độ phức tạp

+ **Time O(n)**
+ **Space O(1)** iterative

## 8. Biến thể

+ **#92** Reverse II (đoạn từ pos left→right)
+ **#25** Reverse Nodes in k-Group
+ **#234** Palindrome Linked List (reverse nửa sau)
