# 1. Architecture

```mermaid

flowchart TD

portal["Portal"]
scheduler["Scheduler"]

pipeline_gateway["Pipeline Gateway]
model_gateway["Model Gateway"]

bootstrap["Bootstrap"]
controller["Controller"]
policy_router["Policy Router"]
bodyguard["Bodyguard"]


```

# 