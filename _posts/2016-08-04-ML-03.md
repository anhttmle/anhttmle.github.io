---
title: "Minimize cost function with Gradient descent algorithm"
date: 2016-08-04
layout: post
categories: AIFundamental
series: "Deep Learning Understanding"
series_part: 3
comments: true
mathjax: true
---

Trong bài viết trước chúng ta đã tìm hiểu về cách tối thiểu hoá hàm số, từ đó áp dụng vào tối thiểu hoá cost function \\(J(\theta)\\). Hôm nay chúng ta sẽ đi vào tìm hiểu về giải thuật **“Gradient descent”** nhằm khắc phục nhược điểm của phương pháp tìm nghiệm của \\(J'(\theta) = 0\\) (đạo hàm của \\(J(\theta)\\)) và giới thiệu về mô hình “**Linear Regression**" (Hồi quy tuyến tính).

## 1. Gradient descent:

Chúng ta đã biết đạo hàm của hàm số là hàm số mô tả sự biến thiên của \\(\text{slope}\\) trên từng điểm của hàm số đó. Bên cạnh đó \\(\text{slope}\\) tại một điểm trên hàm số hay đạo hàm tại điểm đó của hàm số cho ta biết hàm số đang tăng, giảm hay đạt cực đại, cực tiểu. 

<img src="/assets/content_images/ml03-01.png" alt = "" width = "100%">

Phương pháp thông thường trong toán học là ta giải phương trình \\(J'(\theta) = 0\\). Tuy nhiên phương trình không phải lúc nào cũng dễ giải và chi phí để giải phương trình khi số lượng biến lớn cũng khiến performance của hệ thống bị ảnh hưởng. 

<img src="/assets/content_images/ml03-02.png" alt = "" width = "100%">

Vấn để được đặt ra là nếu không giải phương trình, với giá trị \\(\theta\\) hiện tại (chẳng hạn ban đầu tất cả tham số \\(\theta = 0\\)) làm sao để thay đổi \\(\theta\\) để cho \\(J(\theta)\\) nhỏ hơn (chưa phải nhỏ nhất), nếu như lặp lại quá trình này nhiều lần thì ta sẽ chọn được \\(\theta\\) sao cho \\(J(\theta)\\) gần với giá trị nhỏ nhất của \\(J(\theta)\\) hay điểm cực tiểu của hàm số. Giả sử với \\(\theta\\) hiện tại, giá trị của \\(J(\theta)\\) như hình sau: 

<img src="/assets/content_images/ml03-03.png" alt = "" width = "100%">

Với \\(\theta\\) hiện tại, \\(J(\theta)\\) đang ở điểm **A**, rõ ràng ở vị trí **A** đồ thị trên đang có xu hướng giảm (\\(\text{slope}\\) tại điểm **A** \\(< 0\\), nếu tăng \\(\theta\\) thì \\(J(\theta)\\) sẽ giảm). Ta tăng \\(\theta\\) để **A** tiến đến điểm **B** như hình sau: 

<img src="/assets/content_images/ml03-04.png" alt = "" width = "100%">

Khi đó \\(J(\theta)\\) giảm xuống (vì \\(\text{slope}\\) tại **A** \\(< 0\\) thì khi tăng \\(\theta\\) thì \\(J(\theta)\\) sẽ giảm). Vậy với giá trị \\(\theta\\) hiện tại, chúng ta đã biết tăng hay giảm \\(\theta\\) để giá trị của \\(J(\theta)\\) giảm xuống. Thử tưởng tượng rằng chúng ta làm việc này một cơ số lần, khi đó \\(J(\theta)\\) sẽ giảm dần xuống giá trị cực tiểu gần nhất (cực tiểu địa phương - **Local minimum**).

<img src="/assets/content_images/ml03-05.png" alt = "" width = "100%">

##### Giải thuật được mô tả như sau:

* Khởi tạo \\(\theta\\) bất kì (thường tất cả tham số \\(\theta\\) được gán \\(= 0\\)).
* Liên tiếp thay đổi \\(\theta := \theta - \alpha \cdot J'(\theta)\\) đồng thời (tất cả các tham số \\(\theta\\) phải được thay đổi cùng 1 lúc)
* Dừng lại khi \\(J(\theta)\\) có thay đổi không đáng kể.
Trong đó \\(\alpha\\) được gọi là **Learning rate** (dùng để điều chỉnh tốc độ học của máy). Trước khi đi vào giải thích về tham số này, chúng ta nói một chút về nhược điểm của giải thuật này. 

<img src="/assets/content_images/ml03-06.png" alt = "" width = "100%">

* Khi ta đang ở điểm **A**, có thể giải thuật này sẽ đi vào vòng lặp vô hạn do hàm số đi xuống mãi khi giảm \\(\theta\\). Tuy nhiên trong trường hợp này việc giải phương trình cũng không mang lại kết quả do bản thân hàm số \\(J(\theta)\\) không có giá trị nhỏ nhất (**Global minimum**). Cách giải quyết là ta phải xây dựng hàm \\(J(\theta)\\) có giá trị nhỏ nhất. Chẳng hạn:

<img src="/assets/content_images/ml03-07.png" alt = "" width = "100%">

* Khi ta đang ở điểm **B** và điểm **C**, áp dụng giải thuật trên sẽ đưa ta đến 2 điểm cực tiểu khác nhau. Rõ ràng tại **C**, ta sẽ giảm được \\(J(\theta)\\) nhiều hơn, chính vì thế để đảm bảo \\(J(\theta)\\) đạt được giá trị nhỏ nhất, ta phải cố gắng xây dựng \\(J(\theta)\\) có dạng tương tự như parabola. Khi đó \\(J(\theta)\\) sẽ chỉ có một giá trị nhỏ nhất, và giải thuật chắc chắn đưa \\(J(\theta)\\) xuống đáy của đồ thị.
 
<img src="/assets/content_images/ml03-08.png" alt = "" width = "100%">

Trở lại với vấn đề về *Learning rate* \\(\alpha\\). Nếu như ta loại bỏ tham số \\(\alpha\\) thì chẳng qua là để \\(\alpha = 1\\) (giải thuật trở thành \\(\theta := \theta - J'(\theta)\\)). Việc xác định được các tham số \\(\theta\\) nhanh hay chậm phụ thuộc vào tham số này, tuy nhiên nếu như lựa chọn không phù hợp có thể gây ra hệ luỵ nhất định. Cụ thể như sau:
+ \\(\alpha\\) quá nhỏ: \\(\alpha\\) nhỏ sẽ khiến cho các bước đi xuống dốc của \\(J(\theta)\\) cũng nhỏ => việc học mất nhiều thời gian hơn.
+ \\(\alpha\\) quá lớn: \\(\alpha\\) lớn sẽ khiến cho các bước đi trở nên quá lớn, có thể khiến \\(J(\theta)\\) lớn hơn giá trị hiện tại. Như hình vẽ sau:

<img src="/assets/content_images/ml03-09.png" alt = "" width = "50%" align = "center">

## 2. Linear Regression:

Bây giờ chúng ta sẽ đến với một mô hình cụ thể. Đặc biệt đây là mô hình đơn giản nhất trong **ML**, mô hình “Hồi quy tuyến tính” (**Linear Regression**).
Bài toán: Ta có một tập dữ liệu để máy học như sau: 

<img src="/assets/content_images/ml03-10.png" alt = "" width = "100%">
 
Vì là ví dụ nên lượng dữ liệu nhỏ, trên thực tế, chúng ta cần một lượng lớn dữ liệu để có thể xây dựng mô hình chính xác hơn. Ta đưa dữ liệu trên vào đồ thị như sau: 

<img src="/assets/content_images/ml03-11.png" alt = "" width = "100%">

Bây giờ chúng ta xây dựng một hàm số có dạng đồ thị là đường thẳng, và cố gắng khiến nó gần giống với \\(f(x)\\) nhất. Nhắc lại rằng hàm số có đồ thị là đường thẳng có dạng như sau: \\(y = \theta_1 x + \theta_0\\) trong đó \\(x\\) là input và \\(y\\) là output. Trong hình vẽ trên, những điểm được vẽ bởi tập dữ liệu học chính là những điểm nằm trên đồ thị hàm số \\(f(x)\\). Chúng ta bắt đầu với đường thẳng **\\(h(x) = x + 1\\)** (trong trường hợp này **\\(\theta_1 = 1\\)** và **\\(\theta_0 = 1\\)** là các tham số \\(\theta\\) chúng ta vẫn nhắc tới từ trước đến giờ).

<img src="/assets/content_images/ml03-12.png" alt = "" width = "100%">

Ta thấy rằng khi đó \\(J(\theta)\\) sẽ là hàm số 2 biến \\(\theta_1\\) và \\(\theta_0\\), khi đó để mô tả \\(J(\theta)\\) trên đồ thị chúng ta sẽ cần tới đồ thị 3 chiều (\\(\theta_1\\), \\(\theta_0\\) và \\(J(\theta)\\)). Để đơn giản và có thể trình bày trực tiếp trên đồ thị 2 chiều, tôi sẽ bỏ bớt 1 tham số \\(\theta_0\\) hay mặc định cho \\(\theta_0 = 0\\) tương đương với việc để \\(J(\theta)\\) đi qua gốc toạ độ \\((0, 0)\\). Đồ thị của chúng ta sẽ trở thành như sau:

<img src="/assets/content_images/ml03-13.png" alt = "" width = "100%">

Với \\(h(x)\\) như trên thì khi \\(x = 3 \Rightarrow h(x) = 3\\), có vẻ không được chính xác lắm. Ta sẽ thay đổi \\(\theta_1 = 1\\) để \\(h(x)\\) dự đoán được kết quả gần với \\(y = f(x)\\) hơn. Sai số của \\(h(x)\\) và \\(f(x)\\) đơn giản nhất là \\(J = h(x) - f(x) \Rightarrow J(\theta) = \sum[h(x) - f(x)] = \sum[h(x) - y]\\) (được tính trên tất cả dữ liệu chúng ta có cho máy học). Cụ thể trong ví dụ này: \\(J(\theta) = [h(1) - 1] + [h(2) - 2] + [h(3) - 1.3] + [h(4) - 3.75] + [h(5) - 2.25]\\)

Tuy nhiên \\(h(x) - f(x)\\) tuỳ trường hợp sẽ có giá trị âm hoặc dương nên chúng không thể hiện được sự sai khác giữa \\(h(x)\\) và \\(f(x)\\). Nếu lấy giá trị tuyệt đối \\(\lvert h(x) - f(x) \rvert\\) sẽ khiến cho \\(J(\theta)\\) trở nên phức tạp, nên để khắc phục ta bình phương chúng (phương pháp này còn gọi là **Least Square** - tối thiểu hoá phần diện tích hình vuông có cạnh là sự sai khác giữa giá trị dự đoán và giá trị thực tế). 
<img src="/assets/content_images/ml03-14.gif" alt = "" width = "100%">
\\(\Rightarrow J(\theta) = \sum[h(x) - f(x)]^2 = [h(1) - 1]^2 + [h(2) - 2]^2 + [h(3) - 1.3]^2 + [h(4) - 3.75]^2 + [h(5) - 2.25]^2 = 10.515\\)
Giờ chúng ta sẽ làm cho \\(J(\theta)\\) nhỏ đi, để nguyên tham số \\(\theta_1\\) ta có \\(J(\theta) = \sum[\theta_1x - y]^2\\). Hàm số \\(J(\theta)\\) là hàm bậc 2, đồ thị có dạng **parabola** (đúng như trong giải thuật Gradient descent mong muốn). Trước tiên ta sẽ sử dụng phương pháp Normal Equation để tìm \\(J(\theta)\\) nhỏ nhất.
 
$$
\begin{align*}
& J(\theta) \\
& = \sum(\theta_1x - y)^2 \\
& = \sum(\theta_1^2x^2 - 2\theta_1xy + y^2)\\
& = \sum\theta_1^2x^2 - \sum 2\theta_1xy + \sum y^2\\
& = \theta_1^2\left(\sum x^2\right) - \theta_1\left(\sum 2xy\right) + \sum y^2\\
& \Rightarrow J'(\theta) = 2\theta_1\left(\sum x^2\right) - 2\sum xy\\
\end{align*}
$$

**Suy ra**:

$$
\begin{align*}
& J'(\theta) = 0 \\
& \Leftrightarrow 2\theta_1\left(\sum x^2\right) - 2\sum xy = 0 \\
& \Leftrightarrow \theta_1 = \frac{2\sum xy}{2\sum x^2} = \frac{\sum xy}{\sum x^2} \\
\end{align*}
$$

Nhưng vì số lượng dữ liệu đưa vào có thể thay đổi (chẳng hạn khi ta đưa thêm dữ liệu vào cho máy học) nên \\(J(\theta) = \frac{1}{m}\sum[h(x) - f(x)]^2\\) sẽ tổng quát hơn. Bên cạnh đó vì \\(J(\theta)\\) là hàm số bậc 2 nên khi đạo hàm ta sẽ thu được hàm số có dạng \\(2g(x)\\) (vì \\(f(x) = x^2\\) thì \\(f'(x) = 2x\\)), nên để cho gọn ta thêm \\(\frac{1}{2}\\) vào \\(J(\theta) \Rightarrow J(\theta) = \frac{1}{2m} \sum[h(x) - f(x)]^2\\)
Trong ví dụ trên sau khi giải phương trình ta có: \\(\theta = \frac{45.15}{55} \approx 0.82\\). Đưa vào đồ thị ta có: 

<img src="/assets/content_images/ml03-15.png" alt = "" width = "100%">

Trông có vẻ ổn, các bạn hãy thử áp dụng giải thuật Gradient descent với ví dụ này xem có thu được kết quả tương tự không nhé.
Để ví dụ đơn giản nên tôi chỉ đưa ra mô hình “**Linear Regression**” trong trường hợp có 1 tham số \\(\theta\\). Với \\(h(x)\\) không nhất thiết đi qua gốc toạ độ chúng ta sẽ có thêm tham số \\(\theta_0\\), khi đó \\(J(\theta)\\) sẽ không dừng lại ở dạng parabola mà sẽ là hàm số 2 biến phải được mô tả trong không gian 3 chiều và đồ thị của nó có dạng như sau:

<img src="/assets/content_images/ml03-16.png" alt = "" width = "100%">

Đối với không gian \\(\mathbf{n}\\) chiều, \\(\mathbf{x}\\) không chỉ là một giá trị vô hướng mà là một vector với toạ độ trên n trục. Ta có mô hình **Linear Regression** tổng quát như sau:

* Tập dữ liệu với \\(\mathbf{m}\\) cặp dữ liệu:

\\(x = (x_1, x_2, \ldots, x_n)\\)

\\(y = f(x)\\)

* Hàm tiên đoán hypothesis:

\\(h(x) = \theta_0 + \theta_1x_1 + \cdots + \theta_n x_n = \sum \theta_i x_i\\) (trong đó \\(x_0 = 1\\))

* Cost function:

\\(J(\theta) = \frac{1}{2m} \cdot \sum[h(x) - y]^2 = \frac{1}{2m} \cdot \sum\left[\sum \theta_i x_i - y\right]^2\\)

\\(J'(\theta) = \frac{1}{m} \sum\left[\left(\sum \theta_i x_i - y\right)x\right]\\)

Trong đó

\\(J'(\theta_j) = \frac{1}{m} \sum\left[\left(\sum \theta_i x_i - y\right)x_j\right]\\) (\\(x_j\\) là giá trị \\(\mathbf{x}\\) tương ứng trong \\(\theta_j x_j\\))

Đến đây hi vọng các bạn đã nắm được giải thuật Gradient descent và quá trình học trên một mô hình của ML như thế nào. Hẹn gặp lại các bạn trong bài viết sau.
